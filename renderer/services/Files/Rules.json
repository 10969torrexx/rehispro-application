{
    "file": {
        "required": true,
        "type": ["jpeg", "jpg", "png", "pdf"],
        "size": 100,
        "message": {
            "required": "File is required",
            "type": "Only JPG, PNG or PDF file are allowed",
            "size": "File must not exceed 100MB."
        }
    }
}